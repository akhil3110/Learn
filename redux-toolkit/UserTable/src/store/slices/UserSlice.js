import { createSlice } from "@reduxjs/toolkit";

export const userSlice = createSlice({
    name: "users",
    initialState: [],
    reducers: {
        addUser(state,actiom){

        },

        removeUser(state,action){
        },

        deleteUser(state,action){

        }
    }
})